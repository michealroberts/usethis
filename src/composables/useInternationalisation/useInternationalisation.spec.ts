import { describe, expect, it } from 'vitest'

import { useInternationalisation18n } from './useInternationalisation'

describe('useInternationalisation18n', () => {
  it('should be defined', () => {
    expect(useInternationalisation18n).toBeDefined()
  })
})