import { describe, expect, it} from 'vitest'

import { useDegreeFormat } from '../'

describe('useDegreeFormat', () => {
  it('should be defined', () => {
    expect(useDegreeFormat).toBeDefined()
  })
})